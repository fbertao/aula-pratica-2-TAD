#include <stdio.h>
#include <matriz.h>

int main (void) {
    Matriz* matriz = matriz_create(3, 3);

    for (size_t i = 0; i < matriz_row(matriz); i++) (
        for (size_t j = 0; j < matriz_cols(matriz); j++) (
            matriz_
        )
    )
    }
}